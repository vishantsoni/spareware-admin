# TODO

## Completed
- N/A

## Next
- [x] Implement Sub-items UI (model_name → years(year_val) → variants(variant_name)) in `src/components/category/AddCategoryCompo.tsx`
  - [ ] Add state for `subItems: CategoryCreateSubItem[]`
  - [ ] Add nested add/remove UI for model/year/variant
  - [ ] Add basic validation
  - [ ] Submit `sub_items: subItems` (or `[]`)
- [ ] Run `npm run lint` and `npm run build` to verify

